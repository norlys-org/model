type ObservationVector = record {
  i : float32;
  j : float32;
  k : float32;
  lat : float32;
  lon : float32;
};
service : {
  fit_obs : (vec ObservationVector) -> ();
  fit_pred : () -> ();
  predict : () -> (vec float32);
}
